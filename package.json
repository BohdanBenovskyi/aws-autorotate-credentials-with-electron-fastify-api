{
  "name": "aws-autorotate-credentials-with-electron-fastify-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build tsconfig.json && tsc-alias",
    "build:watch": "concurrently --kill-others \"tsc -w\" \"tsc-alias -w\"",
    "start": "node --inspect=0.0.0.0:9229 ./build/server.js",
    "build:start": "npm run build && npm run start",
    "build:start:pm2": "npm run build && pm2-runtime ecosystem.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "pm2": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.2.0",
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.5.4"
  }
}
